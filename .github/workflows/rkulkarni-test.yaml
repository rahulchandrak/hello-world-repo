name: rkulkarni-test
on: push
jobs:
    rkulkarni-test:
        runs-on: ubuntu-latest
        steps:
            - name: test
              run: whoami
            - name: checkout
              uses: actions/checkout@v4
            - name: execute shell script
              run: ./ascii-script.sh